version: "3.8"
services:
  flight-api:
    build: .
    image: flight-api:latest
    container_name: flight-api-container
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - SPRING_PROFILES_ACTIVE=prod
